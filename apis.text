/get_file
    POST:
        - receives binary files from client to process them, then returns a json indicating its result.

/hi
    GET:
        - receives an empty get request, returns json containing 'Hi!' message, to ensure app is working!